{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @betterman/db generate && pnpm --filter @betterman/ingest build && pnpm --filter @betterman/web build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @betterman/web start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
